pl:
  activerecord:
    models:
      <%= singular_table_name %>:
        one: <%= human_name %>
    attributes:
      <%= singular_table_name %>:
<% for attribute in attributes -%>
        <%= attribute.name %>: <%= attribute.human_name %>
<% end -%>
        
